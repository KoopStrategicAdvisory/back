NODE_ENV=development
PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/koop
CLIENT_ORIGIN=http://localhost:5173

# Generate strong secrets, e.g. `openssl rand -base64 32`
ACCESS_TOKEN_SECRET=replace_with_strong_secret
REFRESH_TOKEN_SECRET=replace_with_strong_secret
BCRYPT_SALT_ROUNDS=10
ADMIN_API_KEY=replace_with_a_long_random_key

# Optional overrides
# ACCESS_TOKEN_EXPIRES_IN=15m
# REFRESH_TOKEN_EXPIRES_IN=7d
